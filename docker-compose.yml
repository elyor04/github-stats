services:
  github-stats:
    image: golang:1.22
    container_name: github-stats-server
    restart: always
    ports:
      - "127.0.0.1:8063:8063"
    command: bash -c "go run main.go"
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env
